# no-em-dashes-skill

While loaded, agent-generated text never uses em dashes (U+2014). Retroactive cleanup runs only on scopes the user names explicitly.

See `SKILL.md` for the character table, exceptions, and cleanup rules.
